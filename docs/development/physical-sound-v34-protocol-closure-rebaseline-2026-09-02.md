# Physical sound V34 protocol-closure rebaseline

| Field | Value |
| --- | --- |
| Date | `2026-09-02` |
| Status | `COMPLETE / LOCAL_EVIDENCE_BOUND / ZERO_NEW_TARGET_VALUES` |
| Trigger | [V33 D0 terminal contract reject](physical-sound-v33-d0-fresh-development-tournament-result-2026-09-02.md) |
| Claim | `PROTOCOL_REBASELINE_ONLY / NO_MODEL_QUALITY_REAL_MATERIAL_VALIDATOR_RELEASE_OR_PRODUCT_AUTHORITY` |

## Falsifiable problem

V33 D0 did not answer whether the mode-local spectral residual model
generalizes. Both official processes trained and evaluated development, then
raised because that role contained no exact nodal-contact witness required by
the hard gate. No artifact or metric closure was published. The successor must
make another post-access infrastructure failure mechanically impossible before
spending fresh roles.

## Evidence and competing explanations

| Hypothesis | Evidence | Decision |
| --- | --- | --- |
| H1: the official corpus lacks a required structural witness | `hard_gate_conformance` forces `nodal_zero_exact=false` when its counter is zero; A/B emit that exact failure | Accepted root cause |
| H2: the hard gate should silently pass when no nodal mode exists | That would turn a required causal invariant into a vacuous check | Rejected |
| H3: composition fails to preserve an existing nodal zero | No existing witness reached that comparison; failure is the zero counter, not a nonzero composed gain | Rejected as this failure's cause |
| H4: exact nodal witnesses cannot be constructed | P1 plate/beam controls expose deterministic symmetry nodes; fresh contact sets can include them without target access | Rejected |
| H5: V33 roles can be repaired because no metric was printed | Models trained and development evaluation executed before the raise | Rejected; roles are spent |
| H6: V33 proves the spectral representation is poor | No candidate/control metric or prediction artifact exists | Rejected; quality remains unobserved |

The local frozen owner, process diagnostics and control flow fully discriminate
the failure. Internet search was not run because external prior art cannot
change whether this exact code path counted a witness or opened targets.

## Decision

V34 preserves the untested V33 model topology, feature lift, optimizer, step
count, seeds, controls, metrics and numeric gates. It changes only the experimental
protocol and necessarily creates fresh materials, geometry cells, contacts,
truth coefficients, role identities and commitments. Nothing learned from an
unpublished V33 metric may select a V34 value.

Before any official target materializes, V34 must pass two independent gates:

1. a signal-blind structural census proves every official role contains
   non-vacuous witnesses for every applicable hard invariant, including exact
   nodal contact in every development/holdout generalization stratum;
2. a full-shape discarded-fixture owner proves every scientific terminal
   outcome publishes a bounded atomic closure, while true contract faults fail
   before target access.

The owner must separate three outcomes:

- `Pass`: complete artifacts and candidate freeze;
- `ScientificReject`: complete artifacts, exact failure reasons and no freeze;
- `ContractReject`: no official role access and no partial artifacts.

An exception after official role access is always a terminal protocol failure,
never an invitation to repair and retry.

## Rejected alternatives

- Reuse or repair V33 train/development/holdout identities.
- Drop the nodal-zero gate or accept an empty witness set.
- Change SIREN/GNN/capacity/seed/loss/steps based on the V33 failure.
- Treat an exception signature as equivalent to a scientific reject artifact.
- Open holdout to recover missing development evidence.
- Mix real-source acquisition into the synthetic repair; the S0/S1 power lane
  remains independent and unchanged.

## Smallest next action

Freeze one V34 profile that commits fresh role identities and a machine-readable
hard-gate witness specification without materializing residual targets. Then
implement the census owner twice exactly. No training owner or official target
builder is authorized before that result passes.
