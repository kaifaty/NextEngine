# NSR3-B4E2D7R20R63V earliest residual-image research

Status: `FROZEN / IMPLEMENTATION_NEXT`.

## Question

R63U proves both R63S iteration-8 candidates correct under exact common
semantics. At which immutable direct-PCG state `0..8` does the stronger image
certificate first resolve all signs, and does every later state remain valid?

## Selected replay

Reconstruct the two R63S starts and unchanged direct tangent products,
preconditioners and eight-update recurrences. Convert R63R `Z` once. Certify
the start and every update with the exact R63U residual-image formula.

The certificate is observational: no image, sign, error or pass bit enters
PCG. Execute all eight updates after first pass. Record a separate `has_pass`
bit, first passing state and stability through state 8. Require the state-8
certificate roots to equal R63U exactly and both final sign vectors to agree.

No expected first-pass iteration is frozen. R63M's original-system iteration-2
result is historical evidence only and has no common-operator authority.

## Branch

- Both lanes pass stably: select the later first-pass state as the offline
  common correctness baseline; next freeze a finite image enclosure at all
  states through that boundary.
- Only retained passes: isolate exported factor/start arithmetic.
- Neither passes before state 8: apparatus is inconsistent with R63U and must
  fail closed.

## Ceiling

No runtime stop, early exit, candidate update, stored dense `H/X`, R60 signs,
sparse work, timing, GPU or production authority. R64/R65 remain blocked.
