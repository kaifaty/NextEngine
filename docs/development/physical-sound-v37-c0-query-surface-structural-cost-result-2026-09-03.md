# Physical sound V37 C0 query-surface structural and cost result

| Field | Value |
| --- | --- |
| Date | `2026-09-03` |
| Decision | `PASS / REPEAT_EXACT / ZERO_TRUTH_SCIENTIFIC_TARGET_OR_OFFICIAL_ACCESS` |
| Owner commit | `8478dc0397a86393525e0dfd04e18fdf7ad795ad` |
| Owner SHA-256 | `6563d869a02ea3a8e2b1e4483825dfef718d255c59b1c4a4e9309e58b06f6fb8` |
| Profile SHA-256 | `acf424694564f09cfab0aac36c47c00c9b72cb1d98711f7f57216d4012934def` |
| Claim | `TARGET_FREE_FULL_SHAPE_QUERY_SURFACE_STRUCTURE_REACHABILITY_AND_COST_ONLY / NO_TRUTH_EVALUATION_SCIENTIFIC_TARGET_QUALITY_REAL_MATERIAL_VALIDATOR_RELEASE_ADMISSION_COOKER_DEMO_OR_RUNTIME_AUTHORITY` |

## Outcome

Two independent CPU processes construct the complete V37 A0/F0 structural
question and execute the candidate plus every frozen control on constant
artificial-zero values. All four artifacts, stdout and empty stderr are
byte-identical. No truth formula, scientific target, prior prediction/weight,
real or protected signal, official capability or D0/H0 value is opened.

C0 therefore proves that QSO-v0 is structurally executable inside the frozen
resource envelope. It grants X0 complete-owner and terminal-publication work
only. It makes no model-quality, material, validator, cooker, demo or runtime
claim.

## Full-shape structural closure

The owner reconstructs the exact F0 role commitments, derives P1 analytic
modal fields on a fixed canonical `3 x 3` probe graph and binds each independent
surface query to one of eight canonical triangles. A positive seeded envelope
realizes the already frozen surface-function family without changing P1 nodes
or signs. The same analytic P1 field projected from the declared coarse and
refined source meshes has one exact canonical root.

| Role | Cases | Fields | Probes | Directed edges | Query rows | Exact two-hop pairs |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| Train | `648` | `3,240` | `29,160` | `77,760` | `6,480` | `90,720` |
| Development | `432` | `3,240` | `29,160` | `77,760` | `4,320` | `90,720` |
| Method holdout | `432` | `3,240` | `29,160` | `77,760` | `4,320` | `90,720` |
| Total | `1,512` | `9,720` | `87,480` | `233,280` | `15,120` | `272,160` |

Every query row has exact triangle/barycentric support. Pairwise row
intersection between train, development and method holdout is zero. Field and
row identity roots equal F0. Reversing field/probe/query/context enumeration
does not change any canonical root.

Structural field/remesh roots are:

| Role | Field/remesh root | Query/permutation root |
| --- | --- | --- |
| Train | `ed6197628bb2c16728e3fce904ca8c900a461f6e07cde64cd90f8372a068603f` | `71e00acc5bf08abdceb9bbd4e3c1757fb4ed677d3f65bf004b0d5ffe3a44d58d` |
| Development | `df04c1faf3ec4624f82fda9204c3a28fb428e91ce465212f2ec36a78afdb6ee2` | `6382504393ececbdc0112b242da08e3e49c6df70067931537982e9c93688856e` |
| Method holdout | `fd3a274a98e06b41a5ca06c0c31c8d42d2200fb6ecdb104dde7c0e40e726ee31` | `23e440366e34d524cc3061836e4424903242d38f1f07a429cdf70ffff6ddecf2` |

Both A0 structural lifecycles finish `PreflightPass`: D0-shaped and H0-shaped
traces each perform two provider calls over `10,800` structural rows and access
zero target rows. Their topology roots are respectively
`9ca5dcae…e8d05` and `3654d955…cc04`.

## Reachability and full cost path

Artificial weights prove that the field branch, query trunk, elementwise
field/query interaction and two-hop topology propagation each change the
contact path. This is structural reachability only; the resulting artificial
values are not quality evidence.

The cost oracle executes, in frozen order:

1. QSO-v0;
2. nearest and continuous-local controls;
3. fixed RBF-integral ridge;
4. field-only, query-only, no-topology and no-interaction QSO ablations;
5. the 35-input V36-shaped pointwise MLP.

QSO-v0 and its four neural ablations each contain `12,443` parameters and run
`96` logical steps. The pointwise control contains `1,859` parameters and runs
the same `96` steps. Combined neural capacity is `64,074`, below the frozen
`70,000` ceiling. Every logical batch retains the frozen `1,024` rows; exact
two-way `512`-row gradient accumulation is an execution layout, not a smaller
scientific batch. All three non-neural controls execute all three roles.

The maximum encoded-field cache is `5,598,720` bytes against the frozen
`33,554,432`-byte limit. Cost-output digests are repeat-exact for all nine
paths, but remain artificial-only and cannot select a model.

## Resource-negative control and correction

The first complete local layout retained autograd for all `3,240` fields on
every mini-batch and correctly rejected at `1,278,464 KiB` RSS after `68.50 s`.
No target value was available. C0 then changed execution layout only:

- encode only the unique fields referenced by the logical batch;
- accumulate the same `1,024` rows as two `512`-row microbatches;
- release immutable construction graphs after structural evidence is complete.

The QSO topology, width, role rows, logical batch, steps, controls, artificial
values and every scientific gate remain unchanged. Do not retry the
all-fields-per-mini-batch autograd layout unless a later measured runtime makes
the retained full cache necessary and still proves the same resource ceiling.

## Repeatability and resources

| Observation | Run A | Run B |
| --- | ---: | ---: |
| Exit code | `0` | `0` |
| Artifact files / bytes | `4 / 14,863` | same |
| Tree-manifest SHA-256 | `a27908b8e3185aaadb9e3dae713db14d7da3965b475f1e3aba601a4205ff3214` | same |
| stdout bytes / SHA-256 | `184 / 114ceaab…1ba6` | same |
| stderr bytes / SHA-256 | `0 / e3b0c442…b855` | same |
| Outer wall / max RSS | `33.99 s / 981,252 KiB` | `34.07 s / 968,592 KiB` |

Artifact identities:

| Artifact | SHA-256 |
| --- | --- |
| `cost-oracle.json` | `1ba1d0db36fdbad19a4aac17e72103ce55597880fa8d138f99adcd1bc8b2e883` |
| `evidence.json` | `7cab29520cf136de88f301927f2e2c780ae078d1203ab341b7ce1800fa18c771` |
| `report.json` | `49638857e3c9a4bde7d1f7f8cbebecd6788b286661eb885114908b109e7808c3` |
| `structural-census.json` | `5e91d2860cd68ebcc62c8d79d7474db9ce7a4da1ff2258939fd992ec4dca9f55` |

## Verification

- canonical profile, dependency identities and import/value boundaries:
  `PASS`;
- Ruff `0.16.3` format/check, Python compilation and strict mypy `2.0.0`:
  `PASS`;
- focused C0 suite: `PASS`, `8/8` tests;
- combined V37 A0/F0/C0 suites: `PASS`, `21/21` tests;
- independent process A/B: `PASS / REPEAT_EXACT`, all four files, stdout and
  empty stderr;
- focused xtask physical-sound registry suite: `PASS`, `161/161` tests;
- `cargo run -p xtask -- boundary-scan`: `FAIL` only on the known unchanged
  `SOURCE_LAYOUT_ESCAPE_HATCH` in
  `tools/xtask/src/physical_sound_registry_command/realimpact_transfer_fixture.rs`;
- no Cargo ProductCheck applies because SPEC-45 has no production consumer or
  promoting ADR.

## Consequence and next boundary

C0 authorizes X0 only. X0 must bind materialization, fit, prediction, every
control, metrics, hard/resource gates and atomic terminal publication into one
complete owner. It must mutation-test every lifecycle/access/failpoint path and
prove that only `Pass` can publish a candidate freeze; reject and fault paths
must never return candidate authority. X0 may use surrogate values but cannot
create an official capability or open D0/H0. Authored clips remain the only
production authority.
