# Physical sound V35 F0 geometry-hybrid profile freeze result

| Field | Value |
| --- | --- |
| Date | `2026-09-02` |
| Decision | `PASS / REPEAT_EXACT / ZERO_TARGET_MODEL_PRIOR_VALUE_OR_SIGNAL_ACCESS` |
| Owner commit | `02719255a6bb1999d474c3d8a410d8eaf810dc74` |
| Owner SHA-256 | `5173638f200ded42f50a72258617b69df0225f9a0b8fd3fe092475533e1548b2` |
| Profile SHA-256 | `019cadd51254c27da32e35ab39b35551ba965198f33282feb975af83cb9304cc` |
| Claim | `TARGET_SAFE_SYNTHETIC_GEOMETRY_CONTACT_HYBRID_PROFILE_ONLY / NO_MODEL_QUALITY_REAL_MATERIAL_VALIDATOR_RELEASE_ADMISSION_COOKER_DEMO_OR_RUNTIME_AUTHORITY` |

## Outcome

Two fresh CPU processes ran the committed V35 F0 owner against the same
canonical profile. Both return
`F0_TARGET_SAFE_GEOMETRY_HYBRID_PROFILE_FREEZE_PASS`, authorize only
`V35-C0-signal-blind-witness-and-coverage-census`, produce empty stderr and
publish the same three artifacts and stdout byte-for-byte.

The profile freezes three new materials, ten new geometry cells, `12 / 6 / 6`
pairwise-disjoint train/development/method-holdout contacts and three fresh
truth expressions. It is identity-disjoint from V32, V33 and V34. The inherited
role algebra remains `1,512` cases and `15,120` modal rows. Each evaluation
stratum has `180` algebraic boundary-node candidates; C0 must still prove the
actual P1 witnesses before any target can open.

## Frozen method discriminator

The contact experiment is now immutable:

- the neural expert receives the V34 32-field spectral/local input plus three
  normalized geometry fields;
- the compact three-head candidate has `1,859` parameters: `865` contact,
  `481` decay and `513` global gain;
- decay/global-gain heads, training policy, seed, numeric profile and resource
  bounds are hash-equal to the V34/V33 hypothesis;
- the train-only continuous local control uses all `216` compatible rows in
  each family/support/mode partition, Gaussian weighting, canonical float64
  accumulation and complete case/remesh leave-out for train queries;
- the target-free gate caps local weight at `0.80`, uses continuous nearest
  geometry/contact support distance and rejects beyond normalized squared
  radius `16`;
- nine binding controls include nearest, the continuous local estimator,
  raw/spectral/geometry ridge, the V34-shaped MLP and the geometry neural expert;
  three causal ablations remove geometry, local or neural contribution.

The target-free train contact and geometry bandwidth computations close to
binary64 identities `0x1.e6d4df96cc6b2p-2` and
`0x1.ac6db4c237254p-1`. These are structural coverage values, not learned
parameters or quality metrics.

## Exact access receipt

Every counter is zero in both runs:

| Access | Count |
| --- | ---: |
| Train/development/method-holdout target rows | `0 / 0 / 0` |
| Feature rows materialized | `0` |
| Oracle values evaluated | `0` |
| Model parameters initialized | `0` |
| Prior-generation target/prediction/weight/metric values read | `0 / 0 / 0 / 0` |
| Real/protected signal values decoded | `0 / 0` |
| Network requests | `0` |

F0 spends no V35 scientific role and observes no quality result.

## Repeatability and resources

| Artifact | Bytes | SHA-256 |
| --- | ---: | --- |
| `conformance.json` | `5,984` | `91cdfaa3218d7d98b6656adc4b3e3fc510245713c5c078e073277aa99cf50f6f` |
| `evidence.json` | `1,349` | `9c77e8b3047067f4ffd68d260cb9b43602a2371fc18e5f12a1dff8457a488751` |
| `report.json` | `1,112` | `40f615cc3f785a21f559443fc7464f353893e3e6d25e622a22a0f695fac6b6df` |

The repeated stdout has `194` bytes and SHA-256
`26c6b387a03fb6c5cdc6f692d7af65f6230a4adeeecf6093274b232e0322053f`.
Both runs take `0.05 s`; peak RSS is `23,372 / 23,252 KiB`. Timing and RSS are
external diagnostics and are not part of the exact payload comparison.

## Conclusion and next boundary

F0 proves that V35 is preregistered without target-driven tuning. It does not
show that the hybrid predicts sound well. C0 must now call only P1 and prove
real structural witnesses, finite support and reachability of both experts in
every role/stratum twice exactly. It may not import or call oracle, optimizer,
model-owner or metric code. A missing witness closes the profile before B0 or
official target access.

SPEC-45 remains `Proposed`. No public schema, production content role, runtime
model, real-material claim, validator release, cooker, demo or ProductCheck is
created.

## Verification

- Ruff format/check and Python compile: `PASS`;
- focused V35 F0 Python suite: `PASS`, `6/6` tests;
- official process A/B: `PASS / REPEAT_EXACT`, `3/3` artifacts and stdout;
- focused xtask physical-sound registry suite: `PASS`, `161/161` tests;
- canonical JSON, dependency/profile hashes and direct path/ID checks: `PASS`;
- `git diff --check`: `PASS`;
- `cargo run -p xtask -- boundary-scan`: `FAIL`, known unchanged
  `SOURCE_LAYOUT_ESCAPE_HATCH` in
  `tools/xtask/src/physical_sound_registry_command/realimpact_transfer_fixture.rs`;
- no ProductCheck applies because there is no production consumer or promoting
  ADR.
