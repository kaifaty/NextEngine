# Physical sound V37 D0 fresh development result

## Terminal decision

| Field | Result |
| --- | --- |
| Decision | `MetricReject` in both predetermined processes |
| A/B equality | stdout, empty stderr and complete three-file trees byte-identical |
| Candidate authority | `false`; no bundle or freeze published |
| Method holdout | `0` rows; permanently unopened for QSO-v0 |
| Real/protected signal access | `0` |
| Retry/tuning | Forbidden by the one-shot protocol |

QSO-v0 is closed. This result is a scientific representation reject, not an
owner, hard-physics, resource or determinism failure. It authorizes neither H0
nor a runtime model, real-material validator, cooker, demo or product claim.

## Exact execution

Both processes used frozen profile `2baf41bc…305a`, official provider
`21a901b9…efde`, target-aware owner `ebbdb386…c50c`, T0 seal
`7bae65cc…61573` and D0R seal `a2c3c6ce…b18c5`.

- each process opened `6,480` train plus `4,320` development target rows;
- each evaluated exactly `32,400` target values;
- target roots were `37c06491…7751` for train and `e24a74a3…95ae` for
  development;
- process times were `31.17 s` and `30.95 s`;
- peak RSS was `856,012 KiB` and `835,144 KiB`;
- stdout SHA-256 was `5bc856d8…aeb` for both processes;
- terminal tree was `2ec97691…e0c`, terminal descriptor `3646321e…e50` and
  decision root `6013f906…a729`;
- `evidence.json` was `b3894e97…e7e`; rejected-candidate evidence was
  `5f2c493d…cd13`.

Generated targets, predictions, weights and terminal trees remain external at
`/tmp/nextengine-v37-d0-official.mHnhJ9` and are not committed.

## Scientific result

All `11/11` hard gates and `5/5` resource gates passed. Only `11/22` metric
gates passed.

Candidate aggregate RMSE:

| Axis | QSO-v0 | Best notable control |
| --- | ---: | ---: |
| contact | `0.0316871` | pointwise MLP `0.0204463`; ridge `0.0230926` |
| decay | `0.0100330` | ridge `0.00244791` |
| global gain | `0.00676433` | ridge `0.00141044` |

The contact absolute gate was `<= 0.030`; QSO-v0 missed it. Relative contact
ratios were `0.92555×` continuous-local (required `<= 0.92`), `1.37217×`
ridge, `0.78592×` nearest and `1.54977×` pointwise. Against the best control,
contact-only/geometry-only/joint ratios were respectively `1.82060×`,
`1.50938×` and `1.41867×`, all above the required `0.95`.

The field-only ablation was slightly better than QSO-v0 on aggregate contact
and failed both declared contribution gates: worsening was `0.97323×` on
contact-only and `0.99895×` on joint instead of at least `1.05×`. In contrast,
no-interaction, no-topology and query-only contribution gates passed. This is
evidence that the current field/trunk fusion and separate decay/gain heads do
not exploit the frozen truth structure efficiently; it does not license a
nearby architecture or hyperparameter retry on these spent roles.

## Consequence

- Never rerun, retune or repair V37 QSO-v0 using these roles, targets, metrics
  or predictions.
- H0 remains unopened and permanently closed for this candidate.
- A successor must have a substantively new falsifiable representation and
  fresh target identities before any values are evaluated.
- The independent internet-source and automatic-validator lanes remain useful
  and retain frontier deficits of six exact-Steel and 27 non-Metal groups.

The smallest next action is bounded successor research comparing target-free
architectural hypotheses against the observed failure shape. A selected
successor must then restart the A0/F0/C0/X0/E0/T0/D0R ladder with fresh roles;
no V37 values may be used for model or threshold selection.
