# Physical sound V36 E0 full surrogate rehearsal and seal protocol

| Field | Value |
| --- | --- |
| Date | `2026-09-03` |
| Status | `FROZEN_BEFORE_V36_OFFICIAL_CAPABILITY_OR_TARGET_ACCESS` |
| Parent | [V36 X0 result](physical-sound-v36-x0-mutation-terminal-conformance-result-2026-09-03.md) |
| Claim | `FULL_COUNT_FULL_STEP_DISCARDED_D0_H0_EXACT_OWNER_REHEARSAL_AND_EXECUTION_SEAL_ONLY / NO_OFFICIAL_TARGET_QUALITY_REAL_MATERIAL_VALIDATOR_RELEASE_ADMISSION_COOKER_DEMO_OR_RUNTIME_AUTHORITY` |

## Exact owner boundary

E0 freezes one numeric `execute_owner` entry point. The entry point receives an
A0 `AccessCapability`, an A0 `TargetRoleProvider`, validated immutable context
and, for H0 only, a frozen candidate bundle. It must not inspect whether role
values came from rehearsal or official storage. Provider kind controls access;
it does not select an alternate fit, prediction, metric, gate or publication
implementation.

The D0 path executes the exact A0 stages: train materialization, local
cross-fit, candidate, raw-MLP, V34-shaped spectral MLP and no-geometry fits,
ridge controls, development materialization, hybrid support, predictions,
evaluation, hard gates, resource gates and X0 atomic terminal publication.

The H0 path reconstructs disclosed train support/controls, loads the exact D0
candidate/control bytes, materializes method holdout, predicts, evaluates the
unchanged H0 gates and uses the same hard/resource/publisher boundaries. It
performs zero optimizer steps.

## Surrogate provider

The surrogate provider materializes the same concrete immutable `RoleBatch`
type and exact `6,480 / 4,320 / 4,320` row counts as D0/H0. Geometry, contacts,
P1 modes, features, case spans, strata and row identities follow frozen V36
F0/C0. Train targets are disposable deterministic analytic values. Evaluation
targets are the independently repeated frozen owner prediction plus one fixed
small nonzero residual. This self-consistency fixture makes every ratio and
ablation gate executable without pretending to validate transfer quality. It
neither evaluates nor derives from the fresh V36 truth expressions and has no
scientific authority.

The provider exposes train/development only to D0 and train/method-holdout only
to H0. D0 and H0 rebuild the train container independently and must obtain the
same manifest. No official capability, fresh truth target, prior-generation
numeric artifact, real/protected signal or network path may be reachable.

## Rehearsal and terminal closure

Two fresh processes must each run complete D0 then complete H0. Both primary
paths must naturally return `Pass` under unchanged gates. The resulting files,
stdout, stage/callable traces, role manifests, weights, predictions, metrics,
hard/resource decisions and D0/H0 subtree roots must match byte-for-byte.

After each complete path, E0 reuses its actual full trace to pass the same
bounded payload through `MetricReject`, `HardGateReject` and `ResourceReject`.
Together with the natural `Pass`, this proves every scientific terminal over
the real numeric topology without a test-mode branch inside numeric work.
X0's exception and failpoint evidence remains a bound dependency.

## Execution seal

Only a separate seal builder that observes two byte-identical committed-owner
rehearsal directories may emit the canonical seal. It binds:

- numeric owner, contract, publisher, protocol/profile and all imported numeric
  dependencies by SHA-256;
- effective F0/C0 profile identities and exact D0/H0 subtree roots;
- Python `3.12.13`, NumPy `2.5.2`, Torch `2.13.0+cu130`, CPU float64,
  deterministic algorithms and single intra/inter-op thread settings;
- exact D0/H0 topology hashes, two-run equality and zero forbidden access;
- the unchanged `300 s / 1 GiB / 64 MiB` per-path envelope.

The seal is data, not authority by itself. A later official provider must
recompute every binding before it may construct an official capability. Any
byte or environment drift returns to E0. E0 never constructs that capability.

## Exit and failure

E0 passes only when both committed-owner processes and the seal builder close
exactly. Divergence, a non-Pass primary terminal, missing stage, incomplete
artifact, resource excess or forbidden access produces no seal and leaves D0
blocked. Pre-access implementation defects may be repaired only before a seal;
the complete A0–E0 ladder then repeats.

SPEC-45 remains `Proposed`; authored audio remains the only product authority.
