# Physical sound V36 E0 full surrogate rehearsal and seal result

| Field | Value |
| --- | --- |
| Date | `2026-09-03` |
| Decision | `PASS / REPEAT_EXACT / FULL_D0_H0 / EXECUTION_SEALED / ZERO_OFFICIAL_VALUES` |
| Owner commit | `77d9f21e7edf51f7cd42f17f72bc49e3b477bd12` |
| Owner SHA-256 | `629f929e0ed3b34f8d8d914d5a7ea0830eeed4dfa58504de0f868b21576324ee` |
| Profile SHA-256 | `7932ad1d290a26b6194453119df4b97e2edb1b6cae92dc235440c3b13037f556` |
| Protocol SHA-256 | `8b7784c5f0b8d1dd9bf85db90da05eb787c8e3b4b320b3a7fd648f1c4170041c` |
| Seal payload SHA-256 | `45091c7236a79aa3c8b078a104da597bef1ae5b42c60c3a4e25c33ef556148a8` |
| Seal document SHA-256 | `2d65479e4eb6506b477b75804e15d987f85849744160ad84d18c3f2523b2a124` |
| Claim | `FULL_COUNT_FULL_STEP_DISCARDED_D0_H0_EXACT_OWNER_REHEARSAL_AND_EXECUTION_SEAL_ONLY / NO_OFFICIAL_TARGET_QUALITY_REAL_MATERIAL_VALIDATOR_RELEASE_ADMISSION_COOKER_DEMO_OR_RUNTIME_AUTHORITY` |

## Outcome

The committed E0 owner completed the full unchanged D0 numeric topology and
then the full H0 topology in two fresh CPU processes. The 24-file trees and
stdout match byte-for-byte. A separate builder accepted only those two exact
trees and emitted the checked-in
[execution seal](../../lab/profiles/physical-sound-v36-e0-execution-seal.v1.json).

E0 exercised real training, controls, prediction, metric, hard-gate,
resource-gate and atomic-publication code, but only with disposable synthetic
values. It constructed no official capability and evaluated no fresh V36 truth
target. Its intentionally transparent self-consistency evaluation fixture is a
pipeline conformance load, not evidence that the hybrid predicts physical
sound well.

## Full path closure

| Path | Train rows | Evaluation rows | Optimizer work | Files | Artifact root |
| --- | ---: | ---: | --- | ---: | --- |
| D0 | `6,480` | `4,320` development | candidate, raw MLP, V34-shaped MLP and no-geometry model, each `1,200` steps | `12` | `68b6e2754dd2da30280e9c8b468bbe825396e4ce53255ce5d30324f1df7fd5c7` |
| H0 | `6,480` reconstructed | `4,320` method holdout | `0` steps; exact frozen D0 bytes loaded | `8` | `695bb835be8a9dd8ceabe2edf912ba583f617a29e6cb17ec23443eba2e92ee27` |

The H0 train manifest equals the D0 train manifest byte-for-byte. D0 passes all
`28` metric/ablation gates and H0 all `14`; both pass all `20` inherited and
V36 hard checks plus both resource checks. The candidate aggregate normalized
RMSE is `0.0004052481031069644 / 0.0004052481031069645` for D0/H0 and contact
RMSE is `0.00007710503820589998 / 0.00007710503820590005`. These deliberately
small values only confirm the declared self-consistency fixture and carry no
scientific-quality authority.

The natural `Pass` trace for each path was replayed through `MetricReject`,
`HardGateReject` and `ResourceReject`. All eight D0/H0 scientific-terminal
directories publish atomically with the exact frozen topology:

- D0 topology: `03f704eb262dfeb34fcfc763ac3d43bff7fb75cb87270c9f1e773a668eb34df6`;
- H0 topology: `7a4a9b90c077dc5d4a0a0320354bc5c3902bfef624fb5a58c8fb12a2a51d1cee`.

## Access and seal closure

D0 accounts for exactly two provider calls and `6,480 + 4,320` discarded
target rows. H0 independently accounts for `6,480 + 4,320` discarded rows.
Both have zero official D0/H0 rows, fresh-truth evaluations, prior-generation
numeric reads, real/protected signal decodes and network requests. The seal
therefore records `forbidden_access_count = 0` and status
`SealedBeforeOfficialAccess`.

The seal binds the owner and profile bytes, pinned environment hash
`87d630f1bb8234f58ad65316b3185b7608a5a3c1cf92230d934ecada8326027a`,
both topology hashes, both artifact roots, two-run equality and rehearsal tree
root `d4a93280fc6c8bebf638a3b0460ff7a07778cc055572c4a040b6a239cadf6971`.
Any later byte or environment drift invalidates it before official access.

## Repeatability and resources

The exact tree contains `24` files and `1,020,448` bytes. Repeated stdout has
`937` bytes and SHA-256
`a3988a37f12642b6abcee826ece817ee998ec4f6a1cfc66504f2c7cca24ff2c8`.
The candidate weights are
`cdefdf6ea4b1dea0948af62585c307f09e6332c5cdf1fbf54ec50bbff728e410`;
development and H0 prediction hashes are respectively
`1ac8cbfff42b2f6a240f5d2fa5ff705fa6fc2602c6a8c33f0407de369cf57ac5`
and `2969edc318305a1247e854493a45861961aaa7770c3cdaae814c3ea4392f543b`.

Runs take `293.61 / 291.48 s`; maximum RSS is `917,268 / 934,800 KiB`.
Both internal path gates and external process measurements remain inside the
frozen `300 s / 1 GiB / 64 MiB` per-path envelope. Timing and RSS are external
diagnostics and do not enter exact payloads.

## Pre-seal defect handling

The first uncommitted full smoke completed both numeric paths but failed closed
when the new terminal-matrix parent directory was absent. The directory
contract was repaired, covered by a regression test and the entire smoke was
rerun before commit. A second test makes nested symlinks fail instead of being
silently omitted from seal comparison. No official capability or target value
existed during either correction.

## Conclusion and next boundary

E0 closes full-count/full-step owner execution and reproducibility before any
official value opens. D0 may now implement a verifier which recomputes every
checked-in seal binding and, only on exact agreement, constructs the fresh D0
provider capability. The official D0 run remains one-shot: any post-access
Pass, reject or owner fault is terminal for that role, and a code/profile drift
before access returns to E0 rather than being waived.

SPEC-45 remains `Proposed`. E0 creates no physical-quality, real-material,
validator, admission, cooker, demo, runtime or ProductCheck authority; authored
audio remains the product fallback.

## Verification

- Ruff format/check and Python compile: `PASS`;
- strict mypy on the E0 owner with skipped imported internals: `PASS`;
- focused E0 suite: `PASS`, `8/8` tests;
- combined V36 Python suites: `PASS`, `46/46` tests;
- committed-owner processes A/B: `PASS / REPEAT_EXACT`, `24/24` files and stdout;
- separate seal builder plus independent recursive diff: `PASS`;
- focused xtask physical-sound registry suite: `PASS`, `161/161` tests;
- `cargo run -p xtask -- boundary-scan`: `FAIL`, the known unchanged
  `SOURCE_LAYOUT_ESCAPE_HATCH` in
  `tools/xtask/src/physical_sound_registry_command/realimpact_transfer_fixture.rs`;
- no ProductCheck applies because there is no production consumer or promoting
  ADR.
