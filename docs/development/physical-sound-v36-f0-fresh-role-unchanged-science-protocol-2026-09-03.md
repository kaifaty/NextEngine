# Physical sound V36 F0 fresh-role and unchanged-science protocol

| Field | Value |
| --- | --- |
| Date | `2026-09-03` |
| Status | `FROZEN_BEFORE_V36_TARGET_MODEL_OR_METRIC_ACCESS` |
| Parent | [V36 roadmap](../plans/physical-sound-synthesis-roadmap-v36.md) |
| Scientific parent | [V35 F0 result](physical-sound-v35-f0-geometry-hybrid-profile-freeze-result-2026-09-02.md) |
| Owner contract | [V36 A0 result](physical-sound-v36-a0-typed-owner-contract-result-2026-09-03.md) |
| Claim | `TARGET_SAFE_FRESH_ROLE_AND_UNCHANGED_SCIENCE_PROFILE_ONLY / NO_MODEL_QUALITY_REAL_MATERIAL_VALIDATOR_RELEASE_ADMISSION_COOKER_DEMO_OR_RUNTIME_AUTHORITY` |

## Purpose

F0 preregisters the fresh V36 synthetic evidence before any target, prediction,
weight or metric can be observed. It keeps the V35 hypothesis scientifically
unchanged while replacing every one-use evidence identity. A pass authorizes
only a signal-blind structural census in C0.

The freeze evaluates no truth expression, materializes no feature or target
row, initializes no model, decodes no real or protected signal and reads no
prior-generation target, prediction, weight or metric payload.

## Unchanged scientific projection

The V36 effective profile is derived from the hash-bound V35 F0 profile. The
following content must remain canonical-hash equal to V35:

- fixture families, base fixture, impulse, mode count, role/count algebra and
  every non-identity corpus field;
- numeric profile, resources and the complete training policy, including model
  seeds `3301 / 3302`;
- all feature definitions, compact topology, controls, local expert, coverage
  gate, causal ablations and development/method-holdout thresholds;
- oracle bounds, component definitions and transcendental implementation;
- the V35 witness thresholds other than the explicitly fresh train-permutation
  seeds.

The comparison is performed on one canonical projection obtained by removing
only the declared identity paths. A second comparison retains the oracle
structure while removing only its three coefficient-bearing expressions. Any
other difference rejects F0 before access.

## Fresh evidence identities

The committed V36 overlay contains:

- materials `synthetic-m/n/o`, with complete tuples disjoint from V32–V35;
- ten new geometry multiplier cells, disjoint from V32–V35;
- `12 / 6 / 6` train/development/method-holdout contact pairs, mutually
  disjoint and disjoint from V32–V35;
- one exact `u=0` boundary-node candidate in every contact set;
- three new coefficient-bearing truth expressions, structurally identical in
  bounds/components/transcendentals but byte-disjoint from every V32–V35 truth;
- role prefix `physical-sound-v36-fresh-role-unchanged-science-v1`;
- fresh case/role/truth namespace seeds and B0 permutation seeds. These seeds
  identify evidence only; they do not replace the unchanged model seeds.

Every enumerated case identity commits to the role prefix, role seed, stratum,
family, material tuple, geometry cell, contact pair and modal ordinal. The F0
owner enumerates all `1,512` case commitments and `15,120` row commitments
without evaluating features or targets, then proves pairwise role separation
and absence from prior-generation identity sets.

## Access and authority

The immutable V36 order is F0, C0, X0, E0, D0 and H0. F0 can inspect only
canonical profile metadata and hashes. Its exact access ledger is all zero:

```text
fresh V36 train/development/method-holdout target rows = 0 / 0 / 0
oracle values evaluated = 0
feature rows materialized = 0
model parameters initialized = 0
prior target/prediction/weight/metric values read = 0 / 0 / 0 / 0
real/protected signal values decoded = 0 / 0
network requests = 0
```

F0 must run in two fresh processes and publish byte-identical conformance,
identity commitments, evidence and report artifacts. Invalid input or an
occupied/symlinked output path publishes no partial directory. Pass authorizes
only `V36-C0-fresh-zero-target-structural-census`; it grants no model execution,
quality, admission, release or runtime authority.

SPEC-45 remains `Proposed`; authored clips remain the only authority.
