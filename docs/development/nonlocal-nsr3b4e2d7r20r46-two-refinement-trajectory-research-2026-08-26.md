# NSR3-B4E2D7R20R46 two-refinement trajectory research

Status: `RESEARCH COMPLETE / TWO TARGET-BOUND REPLACEMENTS SELECTED`.

## Question

Does the unchanged torsion NNQP progress after consuming the first depth-eight
and second depth-16 centered certificates, and what is its next exact boundary?

## Candidate

Keep one default-null hook with two immutable target tuples:

- original R41 tuple -> practical depth eight;
- second R43/R45 tuple -> practical depth 16.

Each target can match and replace at most once. All other failed inverse audits
are rejected and captured without mutation. The second practical certificate
must reproduce R45's radius/sign/separation values; exact big integers remain
offline-only.

Depth 16 is the first R45 arithmetic-floor checkpoint. Choosing depth four
would knowingly repeat R42's ratio-budget failure mode; depth 32 adds work with
no tighter certificate.

## Hypotheses

| ID | hypothesis | discriminator |
|---|---|---|
| V1 | two centered repairs close torsion | exactly two replacements and final torsion certification |
| V2 | active-set progression exposes another inverse target | more transitions, then a distinct fail-closed inverse audit |
| V3 | a different NNQP/globalization boundary appears | both replacements consumed, then non-inverse exact failure |
| V4 | practical second certificate diverges from R45 | second replacement rejected before mutation |

R46 remains target-bound and default-off. It does not address counterflow or
authorize a case-agnostic production mechanism.
