# NSR3-B4E2D7R19R65 best composed-dual path research

Date: `2026-08-25`

Status: `RESEARCH COMPLETE / TRANSACTION CONTRACT FROZEN / IMPLEMENTATION NEXT`.

## Selected design

Retain the v6 15-HVP face-PCG direction and complete 16-value dyadic projected
path. Replace only the incorrect strict baseline-primal-inertia gate with the
validated completed-square dual value:

```text
d_alt(lambda) = 0.5||s-z||^2 + lambda^T c
              + (A^T lambda)^T t - 0.5||A^T lambda||^2,

z = t-A^T lambda,
s = P_D(z).
```

Among candidates with strict fixed-density dual decrease, strict positive
cached-normal physical reduction and strict positive composed-dual change,
select the largest dual change. Ties retain the first stable exponent. No
filter margin or feasibility norm participates in selection.

The completed-square form needs only vectors and the joint projection already
computed for v6. It adds no sparse `A` action. After applying the candidate,
the existing fresh all-row R61 audit provides `raw`; recompute the direct dual
and require prediction/commit agreement under the v9 formula bound.

## Falsifiable goal

Run exactly 16 outer blocks from the immutable R63 payload. Require every
applied line to preserve nonnegative multipliers, joint projection,
stationarity, complementarity, positive normal-reference reduction, exact
recurrence, full-row audit and rollback. Compare terminal maximum raw and
projected-gradient norm with the frozen FISTA pair at explicit sparse work.

This is the first transaction after v9; it may fail even though every parent
candidate ascended dual, because applying candidates changes all later faces
and baselines.

## Outcome routes

```text
strict FISTA KKT dominance at lower sparse work
    -> COMPOSED_DUAL_ACCELERATION_CANDIDATE

all transaction/dual gates pass, but no strict dominance
    -> COMPOSED_DUAL_PATH_REFERENCE_RETAINED

no admissible line or any state/model/correspondence gate fails
    -> COMPOSED_DUAL_TRANSACTION_REJECTED
```

No timing, runtime mutation or production authority is available. Candidate
projection cost and local dual-vector terms are reported separately even when
sparse work remains below FISTA.

Frozen contract:
[best composed-dual path](../plans/nonlocal-nonlinear-solver-research/03b4e2d7r19r65-best-composed-dual-path-contract.md).
