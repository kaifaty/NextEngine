# NSR3-B4E2D7R20R45 second centered-solution research

Status: `RESEARCH COMPLETE / FIXED CHECKPOINT SOLUTION AUDIT SELECTED`.

## Question

At which predeclared centered checkpoint, if any, does the second contractive
R44 inverse resolve every sign of its own represented passive solution?

## Method

Keep the exact R43 trajectory and capture immutable. For the second tuple:

1. compute exact/Dot2 `r=b-Ax` and `z=Xr`;
2. reuse the R44 exact/Dot2 left defect `C=I-XA`;
3. generate exactly 32 shadow centers `y<-z+C*y`;
4. independently certify `g=z+C*y-y` and `x+y` at the same frozen depths
   `1,2,4,8,16,32` used by R41;
5. report all signs, radius, separation and exact containment.

No depth may stop generation early. This is a fresh RHS audit, not reuse of
the first target's radius or sign vector.

## Hypotheses

| ID | hypothesis | discriminator |
|---|---|---|
| U1 | centered refinement generalizes to the second RHS | a fixed checkpoint resolves all 65 signs |
| U2 | the second exact solution is face-near | depth 32 still leaves an interval crossing zero |
| U3 | input bounds plateau before sign resolution | later checkpoint radii stop improving with unresolved signs |
| U4 | composed arithmetic is invalid | exact containment or underflow rejection |

U1 authorizes only a later second-replacement trajectory. R45 does not apply
the second center or continue torsion.
