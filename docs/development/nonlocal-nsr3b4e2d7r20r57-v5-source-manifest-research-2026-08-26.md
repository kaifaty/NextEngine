# NSR3-B4E2D7R20R57 v5 source-manifest research

Status: `PASS / SIX BLIND V5 SOURCES IMMUTABLY FROZEN`.

## Question

How should blindness be renewed after the composed policy was designed and
validated on v4?

## Design

Commit six deterministic source-only fixtures before constructing any operator
or observing excitation. Use lattice sizes absent from v4, 96--120 particles,
and velocity families that combine saddle flow, layered shear, checkerboard
compression, oblique jet/twist, radial expansion and three-face coupling.

All fixtures retain the already validated owned closed-box source format so the
future preflight tests solver generalization rather than a new boundary ABI.
Their dimensions, velocity coefficients, names, roles and provenance are
normative once committed.

## Blindness rule

R57 may compute only positions, velocities, boundary samples and source/
geometry hashes. Operator construction, projection, excitation, KKT, solver and
timing counters must remain literal zero. A later quiet or rejected source is
evidence; it may not be edited or replaced after this commit.

Success authorizes only a separately frozen input-only v5 preflight.

## Outcome

All six finite/owned sources are distinct and repeat byte-identically with 637
particles total. No operator or solver work executed. Their source roots are
now immutable inputs to R58.
