# Physical sound V46 D1 — external corpus compiler result

| Field | Value |
| --- | --- |
| Date | `2026-09-03` |
| Status | `COMPLETE / REPEAT_EXACT / CLAIM_MASKED_INDEX / CONTENT_OBJECTS_UNOPENED` |
| Decision | `CorpusIndexCompiled` |
| Corpus | `154` real records / `80` physical parents / `10` projects, plus `36` Clatter control groups |
| Claim | B0 control-tournament input only; no model, validator, protected, cooker, demo, public-contract or runtime authority |
| Next | B0 lane-aware analytic/Clatter control tournament; independent E0/V0 evidence growth continues |

## Pre-publication seal

Commit `1ee150c8` freezes the final formatted
[owner](../../lab/scripts/physical_sound_v46_d1_corpus_compiler_v1.py),
[profile](../../lab/profiles/physical-sound-v46-d1-corpus-compiler.v1.json),
[protocol](physical-sound-v46-d1-corpus-compiler-protocol-2026-09-03.md)
and focused tests before the final A/B publication.

| Artifact | Bytes | SHA-256 |
| --- | ---: | --- |
| [`physical-sound-v46-d1-corpus-compiler.v1.json`](../../lab/profiles/physical-sound-v46-d1-corpus-compiler.v1.json) | `5,726` | `9f55b936be7bc65c64985c9d1e8229091449fad0585628f350c108742667a47a` |
| [`physical_sound_v46_d1_corpus_compiler_v1.py`](../../lab/scripts/physical_sound_v46_d1_corpus_compiler_v1.py) | `34,914` | `5752777fcd19c161851a58ae4e34d21cbcdd0055f28b805a61134e97157cf763` |
| [Protocol](physical-sound-v46-d1-corpus-compiler-protocol-2026-09-03.md) | `5,459` | `3175974cf391d88598e58d393b22f75018f7278b9038e9d875d16045b10b1fb5` |
| [`test_physical_sound_v46_d1_corpus_compiler_v1.py`](../../lab/tests/test_physical_sound_v46_d1_corpus_compiler_v1.py) | `6,597` | `4dc54a3195d9e4acf289ab71a847f1460dd692a2c0a26972b71c21bc807bac88` |

The canonical profile binds twelve exact external metadata inputs and the D0
profile/result. The owner accepts only one flat external directory, rejects
extra, missing, symlinked, oversized or hash-drifted inputs, and publishes
atomically outside the repository. It has no network, audio, NumPy, ML or
runtime import.

## Compiled evidence lanes

The path-neutral index preserves the upstream evidence claims instead of
coercing everything into one target:

| Lane | Compiled state | Allowed use |
| --- | --- | --- |
| Empirical prior | `84` Clatter rows grouped by exact modal values into `36` controls | B0 comparator only |
| External synthetic modal teacher | `0` rows after D0 | None; NISR/VibraVerse remain untrusted |
| Structural transfer | `0` materialized rows | None until a source-backed fixture exists |
| Real acoustic | `154` reference rows, `80` raw physical parents, `10` projects | Masked real evidence under historical target contracts |
| Validator calibration | `5` rows from `1` project, isolated inside the real index | Existing calibration reference only; insufficient to qualify V0 |

The real rows retain their original C0R pseudo-target or IETeasy NDAC-75 target
schema and content hash. D1 does not pretend either historical target is the
full 189-coordinate Recipe V3. Missing coordinates remain missing.

## Identity and role closure

The compiler proves the C0R role projections are an exact partition and the
IETeasy projection exactly matches its train increment. The resulting counts
are:

- `79` generator-train records;
- `70` generator-development records;
- `5` validator-calibration records;
- `80` unique physical-parent identities;
- `10` unique source projects;
- `308` content-addressed PCM/target references.

No record ID collides. No physical parent, source project or source component
crosses role families. Raw indexed parent count is deliberately separate from
the stricter planning result: only `71/105` parents currently satisfy planning
support, so the deficit remains `34`.

## Bounded metadata access

D1 read `1,298,295` bytes from the twelve bound metadata files. It
canonicalized `3,528` inline numeric modal-control scalars already present in
the admitted C0 metadata to produce the 36 exact Clatter groups.

All payload/value counters remain zero:

- `0` content objects opened or copied;
- `0` PCM samples, waveform bytes or real target values decoded;
- `0` new external-source modal payload values decoded;
- `0` model or protected values read;
- `0` network requests.

The `308` content references are counted and validated as bounded relative
SHA-256-addressed paths, but their referenced objects remain external and
unopened.

## Repeat-exact publication

Two clean runs after commit `1ee150c8` have no recursive byte difference:

| Artifact | Bytes | SHA-256 |
| --- | ---: | --- |
| `access.json` | `566` | `305edc913ec567418c814b40f079fa317591c037d3d7780248200274e3788440` |
| `corpus-index.json` | `255,789` | `b2b2d17f054eb00bbb15f0400a00007ce5b3a7436bd5b264f03d5bb4e25ae5db` |
| `report.json` | `1,773` | `a8b29ae0dcde716f9a30b84a56985e7dd3791de0577909dc989aee65395fe0e1` |

All six publication gates pass: exact input hashes/schemas, claim-lane
separation, D0 zero-row disposition, expected counts, role/project/parent
isolation and closed content objects.

## Verification

- focused D1 suite: `PASS`, `6/6` tests;
- combined V45 R0/T0/C0 plus V46 D0/D1 suite: `PASS`, `38/38` tests;
- final external A/B and recursive byte comparison: `PASS`;
- Ruff `0.14.1` check/format and Python compile: `PASS`;
- canonical profile/dependency bindings, value-based Clatter grouping, inline
  hash drift, D0 promotion attempt, content-address/role leakage, profile
  mutation, repository-output and forbidden-import guards: `PASS`;
- `cargo run -p xtask -- boundary-scan`: `FAIL` only on the pre-existing
  `SOURCE_LAYOUT_ESCAPE_HATCH` in
  `tools/xtask/src/physical_sound_registry_command/realimpact_transfer_fixture.rs`;
  D1 adds no new source-layout escape hatch and receives no boundary-scan
  credit;
- runtime ProductChecks: `NOT_RUN`, because D1 is external research under
  Proposed SPEC-45 and changes no production consumer or public contract.

## Consequence

D1 is complete and B0 may consume the index. The immediate ML-lab question is
now narrower and testable: on compatible observed lanes, does the analytic
owner, the 36-group Clatter prior, the historical global median or a simple
retrieval/ridge control provide the floor that a later bounded model must beat?

D1 grants no training or model-selection authority. With zero trusted external
synthetic rows and zero materialized structural rows, B0 may honestly return
`NoUsefulTeacher`. E0/V0 still need a second independent descriptor-to-signal
project, `34` more supported real parents and stronger independent validator
power. PSEL, real fitting, protected admission, cooker, demo and runtime remain
blocked; authored clips remain mandatory.
