# Physical sound V36 D0 fresh development protocol

| Field | Value |
| --- | --- |
| Date | `2026-09-03` |
| Status | `FROZEN_BEFORE_OFFICIAL_D0_CAPABILITY_OR_TARGET_ACCESS` |
| Parent | [Corrected V36 E0 seal](physical-sound-v36-e0-full-surrogate-seal-result-2026-09-03.md) |
| Claim | `FRESH_V36_D0_ONE_SHOT_SYNTHETIC_DEVELOPMENT_EVIDENCE_ONLY / NO_METHOD_HOLDOUT_REAL_MATERIAL_VALIDATOR_RELEASE_ADMISSION_COOKER_DEMO_OR_RUNTIME_AUTHORITY` |

## Frozen execution identity

D0 uses the committed E0 `execute_owner` unchanged. The checked-in E0 seal
must validate its document and payload hashes, numeric owner, numeric profile,
complete dependency closure, deterministic environment, D0/H0 topology hashes
and two exact rehearsal roots before an official capability can exist. The D0
profile additionally binds this protocol and the official provider bytes.

Provider kind may select only target authority and publication provenance. It
does not select a model, feature, fit, control, metric, gate or resource path.
The official provider constructs the same immutable `RoleBatch` and structural
identities as E0, replaces its zero structural target transaction with the
three fresh F0 expressions and returns the fully validated batch atomically.
An incomplete provider transaction exposes no role to the owner and produces
no partial artifact.

The official D0 candidate freeze must contain `provider_kind = official-d0`,
the exact provider namespace, status `OfficialD0CandidateFrozenAfterPass` and
the complete verified E0 execution seal. A discarded claim, null seal or zero
official receipt on an official trace is a conformance failure, never a
candidate.

## One-shot roles and access

Two predetermined fresh processes A/B execute the same train/development
experiment for reproducibility. Each process opens exactly:

- train: `6,480` modal rows;
- development: `4,320` modal rows in geometry-only, contact-only and joint
  strata;
- official D0 receipt: `10,800` rows and `32,400` target-axis values;
- method holdout, official H0, prior-generation numeric values, real/protected
  signals and network: exact zero.

The pair is one preregistered experiment, not permission for a retry. Both
processes run after the runner commit with no intervening byte change. Any
post-access `MetricReject`, `HardGateReject`, `ResourceReject`, `OwnerFault`,
process divergence or incomplete artifact permanently spends D0 and leaves H0
unopened. A pre-access `ContractReject` may only report zero target rows and no
output; it returns to the E0/provider boundary without opening D0.

## Unchanged scientific path

D0 retains the exact E0/V35 scientific projection:

1. train candidate, raw MLP, V34-shaped spectral MLP and no-geometry model for
   `1,200` steps each with frozen seeds, float64 CPU and one numerical thread;
2. fit the frozen raw, spectral and geometry-spectral ridge controls plus
   nearest and continuous-local controls;
3. evaluate unchanged aggregate, decay, gain, contact and three transfer-
   stratum gates;
4. require the geometry, local-expert and neural-expert ablation margins;
5. require all P1 nodal/sign/decay/bound/frequency/order/impulse/remesh/render,
   continuity, leave-out, OOD, train-permutation and branch-isolation checks;
6. require the unchanged `300 s / 1 GiB / 64 MiB` per-process envelope and
   atomic terminal publication.

No observed D0 value may choose a seed, capacity, threshold, stencil, feature,
contact, role or retry. Subjective listening cannot waive a miss.

## Exact result and candidate rule

The two output directories and stdout must match byte-for-byte. The result
records the terminal decision, exact access ledger, owner/provider/profile/seal
identities, metrics, hard/resource gates, weights and prediction hashes. Only
a natural repeat-exact `Pass` with every gate true may freeze the one candidate
bundle for H0. H0 receives those exact bytes and performs zero training.

Generated role data, predictions, weights and detailed artifacts stay outside
Git. Only the compact protocol/profile and terminal evidence summary may be
committed. D0 success is synthetic method evidence only; it does not authorize
real Steel, a validator, cooking, a demo, runtime inference or a ProductCheck.

## Pre-access checklist

Before process A starts:

- Ruff, compile, strict owner/provider/runner typing and all V36 focused tests
  pass;
- the checked-in seal equals the separate builder output and the official
  verifier accepts it in two fresh value-free processes;
- target-free train/development structural manifests reproduce the E0 row,
  feature and local-key hashes;
- provider mutations reject before `materialize()` and the exact formula passes
  only artificial-row binary64 fixtures;
- focused xtask registry tests pass and the known unrelated boundary-scan
  escape hatch is reported unchanged;
- Git is clean at the runner commit and all output paths are fresh, external
  and non-symlinked.

SPEC-45 remains `Proposed`; authored audio remains the product fallback.
