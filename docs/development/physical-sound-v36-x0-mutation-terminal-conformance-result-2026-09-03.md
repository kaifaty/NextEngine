# Physical sound V36 X0 mutation and terminal conformance result

| Field | Value |
| --- | --- |
| Date | `2026-09-03` |
| Decision | `PASS / REPEAT_EXACT / 38_MUTATIONS / ALL_TERMINALS_AND_FAILPOINTS_ATOMIC / ZERO_OFFICIAL_VALUES` |
| Owner commit | `920334a16f0b1c4a851b61958ae3be404332bc0a` |
| Owner SHA-256 | `92aa3fd155b3cbf4e95c489b4168de8802ba4074517a966ffe3ca7db77843a0b` |
| Terminal publisher SHA-256 | `b142b2e11d3e4dcd104520639708cd64e080a8eced0cb9903eb4a16448efae81` |
| Protocol SHA-256 | `d8c2a92ab6609e52a8a0ddf45b9ea2353744bb91ea5a11cb08abac305238276f` |
| C0 result SHA-256 | `86a6cef80772ee5571f4ade28c8eac2b451bbbec2451f4e33b120ca16b58dc2d` |
| Claim | `DISCARDED_TYPED_CONTAINER_LIFECYCLE_TRACE_AND_ATOMIC_TERMINAL_CONFORMANCE_ONLY / NO_OFFICIAL_TARGET_MODEL_QUALITY_REAL_MATERIAL_VALIDATOR_RELEASE_ADMISSION_COOKER_DEMO_OR_RUNTIME_AUTHORITY` |

## Outcome

The committed X0 owner ran in two fresh CPU processes and returned
`X0_MUTATION_AND_TERMINAL_CONFORMANCE_PASS` both times. All six output files
and stdout match byte-for-byte. X0 imports the frozen A0 contract and one new
terminal publisher that E0 and later V36 owners must use unchanged.

X0 evaluates only two-row discarded deterministic containers. It never
constructs an official capability, opens a fresh V36 target, initializes a
model, reads an earlier-generation numeric value, decodes real/protected audio
or performs a network request. The result therefore authorizes E0 surrogate
implementation only; it is not scientific evidence for the hybrid.

## Mutation matrix

All `38/38` mutations reject at the intended boundary:

| Boundary | Mutations | Covered failure classes |
| --- | ---: | --- |
| Concrete `RoleBatch` | `13` | schema, writable/non-finite/empty/wrong-row/wrong-width matrices, row/local identities, spans, strata and implicit runtime length |
| Owner lifecycle | `13` | pipeline/provider mismatch, stage skip, role bypass/leak, provider identity, wrong role, double-open and invalid terminal timing/reuse |
| Published trace/access receipt | `12` | ordinal, callable/topology, stage reorder, missing terminal, pipeline/provider, negative/forbidden/role/official access and invalid exception decision |

The separate strict negative fixture fails on exactly one expected diagnostic:
`RoleBatch` is incompatible with `Sized`. The positive strict check accepts the
contract, publisher and X0 owner with no `Any`, unparameterized generic or
unused-ignore diagnostic.

## Terminal and exception closure

Complete discarded D0 and H0 traces publish each scientific terminal:

- `Pass`;
- `MetricReject`;
- `HardGateReject`;
- `ResourceReject`.

Every trace follows the exact A0 stage/callable topology and opens exactly two
role containers. The publisher additionally rejects D0 holdout rows, H0
development rows, surrogate official-row receipts, negative counters and
incomplete scientific role access even when the event sequence is otherwise
well formed.

The four pre-commit failpoints all leave both output and owned staging absent:

1. after staging creation;
2. after the first payload;
3. before the terminal descriptor;
4. before atomic replacement.

Occupied and symlinked outputs, stale staging, an invalid payload leaf, a
repository-local output and byte-budget overflow also fail closed. An injected
unexpected exception before target access becomes `ContractReject` with no
output; the same exception after one discarded role access becomes one atomic
`OwnerFault` directory containing only the fault evidence and terminal
descriptor.

## Exact access receipt

All official counters are zero in both processes: fresh V36 target rows,
official capabilities, model parameters, prior-generation values,
real/protected signal values and network requests. The top-level conformance
trace accounts separately for two discarded train and two discarded
development rows; these carry no scientific or quality authority.

## Repeatability and resources

| Artifact | Bytes | SHA-256 |
| --- | ---: | --- |
| `contract.json` | `1,985` | `b03a2393aed0cb9244bc0d18ddfb4e35b7af42130350edd30f2afc33dafd005e` |
| `evidence.json` | `3,105` | `27eb59c00c7a59e89e1324155161ef47249c1f4927e58d33a1667959be6b076e` |
| `mutations.json` | `1,721` | `9a4f2820ca3beb4bed751b8bea2c035b1f8410d7122e49b779da7cc2687693eb` |
| `publication.json` | `3,127` | `6b717582759fc641c66e97d0c116993b42ab7e8b3b5e811fa7640ea118eebaff` |
| `report.json` | `1,280` | `16ad96c80a157c785141258c28082b55d3e135f77d0efef654d29e94e642deff` |
| `terminal.json` | `3,682` | `3282b5d3be928b89c7e76763bd439ba962b323337503cd0d3c3ebb74567b59f2` |

The six-file tree has `14,900` bytes. Its sorted relative-path SHA manifest is
`6c629fa4244ca6e9e4a7241b25192c432e01b65138d8844f8a558372cc18d0fe`.
Repeated stdout has `1,280` bytes and SHA-256
`16ad96c80a157c785141258c28082b55d3e135f77d0efef654d29e94e642deff`.
Runs take `0.16 / 0.18 s`; maximum RSS is `34,092 / 34,376 KiB`. Timing and RSS
are external diagnostics and do not enter exact payloads.

## Conclusion and next boundary

X0 closes the declared container, lifecycle, access-order, trace, exception and
atomic-publication defect classes before any fresh value opens. E0 may now
implement the actual unchanged D0/H0 numeric owner and execute full-count,
full-step surrogate rehearsals twice exactly. E0 must import the committed
publisher unchanged, exercise all four fits, controls, metrics and gates, then
issue a seal binding both rehearsal roots before an official D0 capability can
exist.

SPEC-45 remains `Proposed`. X0 creates no model-quality, real-material,
validator, admission, cooker, demo, runtime or ProductCheck authority; authored
audio remains the only product fallback.

## Verification

- Ruff format/check and Python compile: `PASS`;
- strict mypy positive boundary: `PASS`, `3/3` files;
- strict negative implicit-length fixture: `PASS`, exactly one expected error;
- focused X0 Python suite: `PASS`, `8/8` tests;
- combined V36 A0/F0/C0/X0 Python suites: `PASS`, `28/28` tests;
- committed-owner process A/B: `PASS / REPEAT_EXACT`, `6/6` files and stdout;
- focused xtask physical-sound registry suite: `PASS`, `161/161` tests;
- `cargo run -p xtask -- boundary-scan`: `FAIL`, the known unchanged
  `SOURCE_LAYOUT_ESCAPE_HATCH` in
  `tools/xtask/src/physical_sound_registry_command/realimpact_transfer_fixture.rs`;
- no ProductCheck applies because there is no production consumer or promoting
  ADR.
