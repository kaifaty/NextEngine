# NSR3-B4E2D7R20R58 v5 input preflight research

Status: `RESEARCH COMPLETE / INPUT-ONLY ADMISSION SELECTED`.

## Question

Do all six immutable R57 sources produce structurally valid, distinctly rooted
and rigorously excited nonlinear constraint inputs?

## Discriminator

Materialize each existing source exactly once through the unchanged sparse
operator path. Verify slot/entry/incidence ownership, source sign convention,
selected low/high row-scale controls, workspace release, distinct problem roots
and the count/max of rows whose projected residual is rigorously positive above
its binary64 bound.

Do not invoke NNQP, semismooth iterations, inverse audits or either R56
certificate hook. An operator failure blocks the lineage. A quiet source is a
valid immutable excitation rejection and must not be edited.

Success with all six excited authorizes a separately frozen one-shot composed
policy replay; any quiet case redirects research to corpus interpretation, not
source tuning.
