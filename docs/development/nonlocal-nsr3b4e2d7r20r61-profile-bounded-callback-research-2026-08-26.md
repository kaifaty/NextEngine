# NSR3-B4E2D7R20R61 profile-bounded generic callback research

Status: `SUPPORTED_BOUNDED / PROFILE_BOUNDED_V5_6_OF_6_CANDIDATE`.

## Question

Does consuming the R60-certified dimension-generic center through the normal
verified-solution hook close the immutable v5 corpus, or reveal a later
nonlinear boundary?

## Selected design

Create a new research-only callback rather than changing R50. It:

- accepts any nonzero active dimension no larger than 120, the already frozen
  maximum v5 problem row count;
- runs the unchanged depth-16 practical certificate without tuple/root/case
  selection;
- derives compensated-dot and input-pair counts from the actual dimension;
- replaces only a fully exact, underflow-free, contractive, sign-resolved
  solution;
- shares neither calls nor cap with the independent R55 slope callback;
- caps successful replacements at the inherited semismooth iteration cap 32.

Replay all six v5 problems once in committed order with fresh callback traces.
Preserve every later active-set, direction, globalization, terminal or cap
boundary without retry.

## Interpretation

- 6/6 certification with exact structural work: profile-bounded composed
  callback candidate;
- first later boundary: preserve it as the next research target;
- dimension/cap/accounting/lifecycle mismatch: apparatus rejection and no
  trajectory claim.

R61 is still binary128 CPU research. It changes no R50 result, runtime/GPU
path, public contract, performance status or production authority.

## Result pointer

The frozen replay certifies 6/6 with one dimension-102 replacement. See
[R61 evidence](nonlocal-nsr3b4e2d7r20r61-profile-bounded-callback-evidence-2026-08-26.md).
