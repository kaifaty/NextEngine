# Physical sound V36 D0 fresh development result

| Field | Value |
| --- | --- |
| Date | `2026-09-03` |
| Decision | `REPEAT_EXACT_METRIC_REJECT / V36_CLOSED / H0_UNOPENED / NO_CANDIDATE_AUTHORITY` |
| Execution repository commit | `3751d999545453a738d5b8592de04c3c4b7e8412` |
| Frozen runner commit | `ed898844ee612a3c4843bbade3cc11992f0d797d` |
| Runner SHA-256 | `14c846af0a7d96821152d7ea9fe3633ce191659a4f113f19763566cf9b1618a7` |
| Provider SHA-256 | `fd83b798e6944316ea0d8e6080e6c9dc1c70f5ed31141d6dbb35324a3138ede2` |
| Profile SHA-256 | `f15835e665fb3a511dcda962e395d2db219ade0dc7a580cf9880ecedb4224456` |
| Protocol SHA-256 | `46e9ef833f9ead016506cf703da83eee2985235e75142b0ec0acdbe82a111821` |
| Seal payload SHA-256 | `b436869654d833f97774b30683bf92672c62529e92a8c5957ee600a91d3ee516` |
| Claim | `FRESH_V36_D0_ONE_SHOT_SYNTHETIC_DEVELOPMENT_EVIDENCE_ONLY / NO_METHOD_HOLDOUT_REAL_MATERIAL_VALIDATOR_RELEASE_ADMISSION_COOKER_DEMO_OR_RUNTIME_AUTHORITY` |

## Outcome

The sealed V36 owner opened the fresh train/development role once as the two
predetermined A/B processes. Both processes completed the full training,
control, metric, hard-gate, resource-gate and atomic-publication path and
returned the same `MetricReject`. Stdout, stderr and every one of the 12
published files match byte-for-byte.

V36 therefore answers the previously unobserved scientific question. The
geometry-conditioned local/neural hybrid learns the decay and global-gain
branches, but its contact transfer is materially worse than the frozen
controls. This is not an execution, resource, access or determinism failure.
The compact V36 hybrid is closed on fresh evidence, no candidate is admitted
and method holdout H0 remains unopened permanently.

## Exact repeat and access closure

| Evidence | A | B |
| --- | --- | --- |
| Process exit | `1` | `1` |
| Decision | `MetricReject` | `MetricReject` |
| Tree SHA-256 | `97c3988154e14c28f980d5890737b5af54aba53844e1bbf19e774391c66f7906` | same |
| Terminal SHA-256 | `ec1b0dc28d91bda1e486ac5f1aab121ea5478cb75671b3c94416b938eddc4d1a` | same |
| Stdout SHA-256 | `93dd76df78d9b5121779d7704ce6b7c979cc8a2481feaf2fec34326312d02e63` | same |
| Published files / bytes | `12 / 535,691` | same |
| Wall time | `150.15 s` | `150.91 s` |
| Maximum RSS | `924,300 KiB` | `924,616 KiB` |

Each process opens exactly `6,480` train and `4,320` development modal rows,
for `10,800` official D0 rows and `32,400` evaluated target-axis values.
Provider calls are exactly two. Method holdout, official H0, prior-generation
numeric values, real/protected signal values, network requests and forbidden
access are all exact zero. H0 output and partial staging paths are absent.

The external detailed evidence root for this execution is
`/tmp/nextengine-v36-d0-official-3751d999.N1vTkj`; generated role values,
weights and predictions remain outside Git.

## Scientific result

All 20 hard checks and both resource checks pass. The metric/ablation matrix
passes `10/28` gates and fails `18/28`, so the decision is unambiguously a
metric reject.

| Metric | V36 candidate | Nearest | Continuous local | Raw MLP | V34-shaped MLP |
| --- | ---: | ---: | ---: | ---: | ---: |
| Aggregate normalized RMSE | `0.131205` | `0.121067` | `0.274613` | `0.110773` | `0.076145` |
| Contact RMSE | `0.063187` | `0.026384` | `0.031148` | `0.033575` | `0.018351` |
| Decay RMSE | `0.000852` | `0.002295` | `0.084858` | `0.001733` | `0.000892` |
| Global-gain RMSE | `0.016343` | `0.037088` | `0.041275` | `0.027367` | `0.022490` |

The contact branch is `2.394927x` nearest, `2.028631x` continuous local and
`3.443194x` the V34-shaped MLP. Against the best control per transfer stratum,
the ratios are:

- contact-only: `1.344382x`;
- geometry-only: `4.195670x`;
- joint: `3.550940x`.

The candidate's absolute contact RMSE is `0.063187`; geometry-only and joint
are `0.069027 / 0.076945`. In contrast, decay and global gain improve over
nearest by ratios `0.371116x / 0.440647x`. The result localizes the failure to
contact-conditioned field transfer and hybrid combination rather than to the
global material branches.

The ablations agree with that interpretation. Removing the local expert hurts
geometry-only and removing the neural expert hurts contact-only, so both carry
signal. But removing the neural expert improves joint transfer
(`0.431428x` candidate ratio), while removing explicit geometry improves the
geometry-only branch (`0.256639x`). The fixed gate combines useful specialists
in the wrong representation/interaction space; a nearby weight or threshold
change cannot be selected from these spent values.

## Reject-artifact provenance defect

The authoritative process report says `candidate_frozen = false`, the owner
returns no `CandidateBundle`, the terminal decision is `MetricReject`, and no
H0 capability or role exists. Those facts correctly prevent candidate use.

The reject evidence tree nevertheless contains trained weights and a file
named `candidate-freeze.json` whose internal status is
`OfficialD0CandidateFrozenAfterPass`. The numerical owner constructs that
document before it conditionally returns a bundle only on `Pass`. On a reject,
the file is useful reproducibility evidence but its name/status overstates its
authority.

This is recorded as
`POST_ACCESS_NONAUTHORITATIVE_FREEZE_METADATA_DEFECT`. It does not turn the
repeat-exact metric reject into an owner fault or make a candidate available,
but the file must never be consumed as an H0 freeze. Because the defect was
observed after fresh target access, V36 code is not repaired or rerun. A
successor experiment framework must publish a freeze document only on `Pass`,
or use an explicitly rejected-candidate evidence schema, and must mutation-test
that distinction before any new protected role opens.

## Consequence and next boundary

1. Do not run V36 H0 and do not reuse any V36 train/development target,
   prediction, weight or metric as a tuning surface.
2. Close the compact coordinate/listener-kernel hybrid family represented by
   V36. Decay/global-gain learning remains positive evidence, not admission.
3. Research a materially different contact field: a topology/surface operator
   with explicit query interaction, rather than another scalar gate over local
   and neural experts.
4. Preserve P1's exact modal/causal ownership and the independent
   internet-source/validator lanes. Neither depends on V36 candidate success.
5. Before allocating new synthetic roles, reuse the sealed owner lessons and
   eliminate reject/freeze ambiguity in a value-free complete-path fixture.

This result has no real-material, perceptual-naturalness, validator-release,
admission, cooker, demo, runtime or ProductCheck authority. SPEC-45 remains
`Proposed` and authored clips remain the product fallback.

## Verification

- final clean-tree pre-access Ruff/compile/strict-mypy checks: `PASS`;
- combined V36 focused suite: `PASS`, `63/63` tests;
- value-free official preparation A/B: `PASS / BYTE_EXACT`, stdout SHA-256
  `dd8ef982079be57f5ec887f87853875b5d7fb7a36263eb823e6e8dd278da7012`;
- focused xtask physical-sound registry suite: `PASS`, `161/161` tests;
- official D0 A/B: `REPEAT_EXACT_METRIC_REJECT`, all 12 files plus stdout and
  stderr exact;
- `cargo run -p xtask -- boundary-scan`: `FAIL`, the known unchanged
  `SOURCE_LAYOUT_ESCAPE_HATCH` in
  `tools/xtask/src/physical_sound_registry_command/realimpact_transfer_fixture.rs`;
- no ProductCheck applies because no production consumer or promoting ADR
  exists.
