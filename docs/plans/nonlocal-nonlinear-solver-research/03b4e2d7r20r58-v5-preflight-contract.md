# NSR3-B4E2D7R20R58 v5 preflight contract

Status: `FROZEN / INPUT-ONLY OPERATOR PREFLIGHT AUTHORIZED`.

## Parent

- R57 implementation `db2ac696`, manifest semantic
  `2e5b935306919c0797a4f3b6f77edd8dfc4dcead3281ed274fc5b7b885523b9a`;
- six immutable source roots in R57 evidence;
- no v5 operator or excitation has been observed.

## Frozen checks

For all six source fixtures, execute `al_r20_materialize_problem` exactly once.
Require exact slot/entry/incidence structures, `source_positive==0`, finite
targets/constraints/domain/operator, selected scale controls at rows
`0/mid/last` for both inherited low/high factors, exact lifecycle release and
pairwise-distinct problem roots.

For each row count `projected_raw > row_bound`; report count and maximum strict
margin. Classify `V5_PREFLIGHT_PASS` only if every case has at least one such
row. A structurally exact quiet corpus returns
`V5_PREFLIGHT_EXCITATION_REJECTED` with PASS apparatus status and no permission
to edit sources.

Routes in precedence:

1. `V5_PREFLIGHT_PARENT_REJECTED`;
2. `V5_PREFLIGHT_OPERATOR_REJECTED`;
3. `V5_PREFLIGHT_EXCITATION_REJECTED`;
4. `V5_PREFLIGHT_PASS`.

Bind every target/constraint/domain/operator/problem/diagnostic root. Solver,
candidate, inverse, certificate and timing counts remain zero. No solver,
generalization, runtime/GPU or production authority.
