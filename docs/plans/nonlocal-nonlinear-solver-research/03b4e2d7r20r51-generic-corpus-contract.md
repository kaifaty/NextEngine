# NSR3-B4E2D7R20R51 generic verifier corpus contract

Status: `FROZEN / FIVE-CASE DEFAULT-OFF REPLAY AUTHORIZED`.

## Parent

- R50 implementation `8d50c59e`, semantic
  `190ac441077ac0499e5e9e4a9c419ef0d604bf6aa06017b49d3051ff7e12d86e`;
- five root-agnostic replacements, `887` transitions and ordinary torsion
  certification;
- work `27,625` dots / `1,802,450` pairs within the frozen per-case cap;
- R35 baseline semantic `59c32c19...37b3` has three ordinary cases, torsion
  inverse rejection and counterflow direction rejection.

## Frozen execution

Replay all five immutable v4 cases once in manifest order. Each case receives
a fresh R50 trace and per-case cap 32. Decisions may use only dimension, cap and
newly computed certificate predicates. Require exact workspace lifecycle,
per-certificate and aggregate structural work ledgers and post-decision torsion
R50 regression. Record all case roots/routes and failures.

Routes in precedence:

1. `GENERIC_CORPUS_PARENT_REJECTED`;
2. `GENERIC_CORPUS_POLICY_REJECTED`;
3. `GENERIC_CORPUS_WORK_REJECTED`;
4. `GENERIC_CORPUS_MULTI_BOUNDARY`;
5. `GENERIC_CORPUS_COUNTERFLOW_ONLY_BOUNDARY`;
6. `GENERIC_CORPUS_5_OF_5_CANDIDATE`.

Regress R50/R49. R51 changes no factorization, inverse generation, direction
bound, ratio, tolerance, cap, globalization, trial, state, counterflow policy,
runtime or timing. It grants no production authority.
