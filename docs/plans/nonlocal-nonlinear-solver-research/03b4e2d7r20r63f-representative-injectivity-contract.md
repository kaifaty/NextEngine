# NSR3-B4E2D7R20R63F representative-injectivity contract -- revision 1

| Field | Value |
|---|---|
| Research ID | `NSR3-B4E2D7R20R63F` |
| Architecture snapshot | R63E semantic `2c9e75c4...16002`; clamp numerical rank 101; all represented component Grams exact rank 102 |
| Engineering consumer | Decide whether projection-equivalent representative selection has an exact local kernel premise |
| Claim class | Exact represented rectangular-rank proof plus bounded tangent/Gram correspondence |
| Claim status target | Local representative nullspace refuted/candidate/inconclusive or exact apparatus boundary |
| Budget | One capture-only parent replay; one `102 x 315` tangent matrix; two fixed modular eliminations; one Gram identity; inherited witness only; no QR, RHS, rank action, center, trajectory or timing |

## Exact claim

The R63E capture and bit-exact full Gram reconstruction repeat. The 102 tangent
row images `t_i=T b_i` are flattened into a finite `102 x 315` binary128 matrix
in source-row/scalar order and bound to an immutable root.

Deterministic modular elimination under the two frozen odd primes establishes
the row rank of this rectangular represented operator. Full rank under either
prime proves that its represented rational kernel is trivial. A deficient
prime image alone cannot prove a kernel.

The independently accumulated image Gram `t_i^T t_j` agrees with the R63E
tangent Gram `b_i^T T b_j` inside a fixed gamma bound, and the inherited
near-null `alpha` produces the same nonzero tangent image through both direct
row combination and R63E witness decomposition.

## Exact negation

The first platform, parent, context, projector, source-row, tangent-image,
matrix-root, modular, Gram identity, inherited witness, nonzero-image, work,
lifecycle or control gate fails. Numerical smallness cannot be converted into
an exact nullspace claim.

## Frozen construction

- Reuse the immutable 105-particle/315-scalar/102-row R63E subject and capture
  lifecycle.
- For each selected row, call the frozen R63E decomposition and flatten its
  `tangent` vector. Require 102 row constructions and 32,130 finite scalar
  writes.
- Bind ordered source row IDs, projector material root, tangent dense root,
  nonzero scalar count and construction work.
- Run `al_r20_r63d_modular_rank` directly on the rectangular tangent matrix for
  `2^61-1` and `2^31-1`; require 32,130 mapped coefficients per prime.

## Independent Gram identity

- Compute all 10,404 `t_i^T t_j` dots with the existing binary128 accumulator.
- Compare them entrywise against the already reconstructed symmetric R63E
  tangent Gram.
- For each entry use a forward bound
  `gamma(32*315+8192) * max(1, absolute_product_sum)`; require all 10,404
  contained and publish maximum residual/bound plus roots.
- This validates the rectangular square-root relation only; it is not a normal-
  equation solve or rank selector.

## Inherited witness

- Reconstruct the same normalized-full pivot witness and unscaled `alpha` as
  R63E; require its alpha root and tangent decomposition root to match the
  current replay.
- Form `u=sum_i alpha_i t_i` independently with 32,130 accumulator terms.
- Compare `u` componentwise with the R63E witness tangent vector under
  `gamma(64*102+8192)` times a fixed absolute-sum scale.
- Require both squared norms finite, mutually contained and strictly greater
  than zero. A nonzero order-`1e-30` norm is near-null evidence, not exact-null
  evidence.

## Controls

1. Literal full-row-rank and duplicate-row rectangular dyadic matrices return
   ranks two and one under both primes.
2. A literal orthogonal projector and two-row operator close
   `b_i^T T b_j=(T b_i)^T(T b_j)` exactly.
3. A literal near-null but nonzero dyadic row combination is not classified as
   an exact kernel.
4. Deliberately shrink one Gram or witness interval; the exact binary128 value
   escapes it.
5. Mutating one tangent scalar or source row changes the bound root.
6. Hook/context lifecycle and R63B--R63E byte regressions pass; RHS, QR/SVD,
   rank action, row drop, center, replacement, state and timing counts are zero.

## Resolution precedence

1. First apparatus/control/correspondence failure.
2. `LOCAL_REPRESENTATIVE_NULLSPACE_REFUTED` if either modular rank is 102 and
   the inherited tangent witness norm is strictly positive.
3. `LOCAL_REPRESENTATIVE_NULLSPACE_CANDIDATE` only with an independently exact
   nonzero coefficient witness whose tangent image is entrywise zero.
4. `RECTANGULAR_MODULAR_RANK_INCONCLUSIVE` otherwise.

## Does not count

Inferring global uniqueness; adapting the polyhedral extreme-point algorithm;
QR/SVD/LSQR; selecting effective rank 101; deleting a row; fitting a threshold;
regularization/wider runtime arithmetic; NNQP RHS/inverse/center/trajectory;
timing, runtime/GPU or production inference.

## Stop and reconsider

- Full represented rank: stop projection-equivalent-representative work on this
  face and freeze a no-drop rectangular solve/certificate discriminator.
- Exact kernel candidate: separately derive finite projection equivalence and
  dual nonnegativity before changing a representative.
- Inconclusive: add an exact rational kernel/minor witness; do not use numerical
  rank 101 as a substitute.
- R64 and R65 remain blocked for every route.
