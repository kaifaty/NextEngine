# NSR3-B4E2D7R20R25 event-input forward-bound contract

Status: `FROZEN / REPORT-ONLY COMPONENTWISE BOUND AUTHORIZED`.

## Parent

- R24 implementation `74dc6dca`, semantic
  `9f3c90728389047d56f777750f9941a33ced533ef97975405f534e69e6d6d67b`;
- exact R23 semantic `48d97d21...1987`;
- step-32 run `new[0..5], old[6], new[7..64]` and maximum actual/affine
  discrepancy `7.8361392161690741e-36`.

## Frozen audit

Use the alpha-independent local gamma bound derived in the research document.
Entry count and scale must come from the exact sparse transpose ownership for
the predicted particle/axis; no observed discrepancy, mask run or crossing
power may enter the bound.

For all seven events and all 65 existing ladder points, require
`|z_actual-z_affine| <= B_z`. Report maximum discrepancy, bound, containment
margin and the first power with `z_affine-B_z>0`. From that power through 64,
require exactly the predicted new face, unchanged ball and positive rigorous
Armijo. Distinguish containment rejection, no certified side, certified-side
mask rejection and all-seven forward-bound candidates.

No new alpha point, fitted operation factor, solver trial/state update, mask
override, cap/tolerance change, timing, runtime/GPU or production authority.

