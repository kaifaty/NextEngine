# NSR3-B4E2D7R20R26 certified event-side trajectory contract

Status: `FROZEN / OPT-IN 12-CASE CANDIDATE AUTHORIZED`.

## Parent

- R25 implementation `87dab448`, semantic
  `d3544d4255d066245d6055b71baa56e67d70e44fce001ba1da83844a4e62d449`;
- R17 default and opt-in roots, including exact 11/12 candidate lineage;
- R25 all-seven containment and stable-side implications.

## Frozen candidate

Add an opt-in defaulting false. Run the old dyadic line first. Only its
mask-stable acceptance preceded exclusively by crossing rejections opens the
event replacement path. Admit only a unique current-face zero-lower release
inside that exact dyadic bracket with positive affine derivative.

Construct `alpha_cert` from the exact R25 formula and evaluate one trial. It
may replace the old acceptance only with nonnegative multipliers, exact one-
scalar predicted mask change, unchanged ball activity and positive rigorous
Armijo. Any absence, ambiguity or failed predicate commits the old accepted
trial unchanged. Hash event provenance, bound, alpha and replacement decision
into new candidate roots; never alter default roots.

Execute the complete ordered 12-case corpus once. Report certifications,
replacements, fallbacks, extra evaluations, accepted steps, NNQP/refinement
work and final KKT. PASS requires default R8/R17 exact, no historical certified
case regression and all 12 candidate cases certified within cap 32.

No cap/tolerance change, proactive event search, timing, runtime/GPU,
production or generalization authority. A positive result authorizes only
work-neutral event-search research and a new independent holdout.

