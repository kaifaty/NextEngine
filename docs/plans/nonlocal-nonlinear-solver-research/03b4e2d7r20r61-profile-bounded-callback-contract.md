# NSR3-B4E2D7R20R61 research contract -- revision 1

| Field | Value |
|---|---|
| Research ID | `NSR3-B4E2D7R20R61` |
| Parent | R60 implementation `fe9689b4`, semantic `c8f11806...1ae2a`; immutable R57/R58 v5 corpus |
| Engineering consumer | Decide whether the composed centered+slope research policy generalizes over the complete blind v5 corpus |
| Claim class | Finite profile-bound trajectory and work-accounting claim |
| Claim status target | `SUPPORTED_BOUNDED` 6/6 or first exact later boundary |
| Budget | Six solver executions, depth 16, at most 32 successful centered replacements and one worked slope audit per case; no retry or timing |

## Exact claim

With a case/root-agnostic centered callback admitting active dimensions
`1..=120`, the exact six immutable v5 problems all return certified under the
unchanged R35 trajectory policy. Every callback entry has a dimension no
larger than its problem row count; every worked entry exactly matches the
dimension-derived depth-16 ledger; every replacement is backed by an exact,
underflow-free, contractive certificate with all signs resolved. The R55 slope
callback remains independently capped and exactly accounted.

## Exact negation

The first exactly reproduced case remains uncertified after a valid callback
decision, or apparatus/work/lifecycle accounting fails. A later inverse,
ratio, direction, globalization, terminal or cap boundary is retained without
retry and refutes 6/6 for this frozen policy.

## Fixed definitions and model

- v5 problem roots and order: exactly R58/R59.
- Per-problem row counts: `100/108/96/105/108/120`; maximum 120.
- Center depth: 16; arithmetic and recurrence exactly R50/R60.
- Center cap: 32 successful replacements per fresh case trace.
- Slope cap: one worked verified inverse audit per fresh case trace.
- Solver, active-set, line, terminal and event policies: unchanged R56/R59.
- Arithmetic: frozen Linux x86_64 GCC binary128 profile.

## Resolution firewall

- Positive: exact apparatus/work plus 6/6 certified.
- Negative: first exact later policy boundary.
- Does not count: depth 4, a root-selected dimension-102 branch, changing R50,
  increasing 120/32/one-audit caps, retrying a case, altering any source or
  treating wall time as performance.
- Ceiling: six tiny immutable research problems only; no universal theorem,
  runtime/GPU/performance/production claim.

## Controls and evidence plan

1. Materialize all six exact R58 problems and bind row counts/roots.
2. Install fresh independent centered and slope traces per case.
3. Execute each case exactly once.
4. For each centered entry with dimension `d`, require:
   - `d <= problem.rows` and `d <= 120`;
   - dots `d*d + 20*d`;
   - input pairs
     `d*d*(d+1) + d*(d+1) + d*d + 16*d*(d+1) + d*(d+2) + 2*d`;
   - exact/no-underflow certificate and replacement iff passed.
5. Bind case/trace/certificate roots and aggregate work into semantic output.
6. Repeat byte-identically and regress R60/R59/R50.

## Stop and reconsider

- Stop immediately at an apparatus mismatch with no solver credit.
- Preserve the first exact later boundary; do not add a third mechanism.
- Reconsider broader dimensions only after another source-only holdout, not by
  raising 120 inside R61.
