# NSR3-B4E2D7R20R56 composed v4 corpus contract

Status: `FROZEN / FIVE-CASE INDEPENDENT COMPOSITION AUTHORIZED`.

## Parents

- R50 torsion semantic/case `190ac441...d86e` /
  `1afe1d2a...e806`, five centered calls/replacements, 887 transitions;
- R51 unchanged case roots for corner/counterflow/helical/alternating;
- R55 counterflow semantic/case `9684d2ad...fc6e` /
  `5e180721...0195`, one slope audit, 701 transitions;
- all v4 source/problem roots remain immutable.

## Frozen replay

For each of five cases, create independent empty `ALR20R50Trace` and
`ALR20R55SlopeTrace`, install both existing callbacks, execute
`al_r20_r35_case` once, release both hooks and retain both traces. No hook is
selected by case name/index/root.

Require trigger matrix:

| v4 index | centered calls/replacements | slope calls/audits |
|---:|---:|---:|
| 0 torsion | `5/5` | `0/0` |
| 1 corner | `0/0` | `0/0` |
| 2 counterflow | `0/0` | `1/1` |
| 3 helical | `0/0` | `0/0` |
| 4 alternating | `0/0` | `0/0` |

Require exact R50/R55 case roots and exact R51 roots for the three unaffected
cases. Require 5/5 ordinary certification, centered ledger 27,625 dots /
1,802,450 input pairs, slope ledger 66 inverse columns, and zero rejection,
cap hit or extra factorization.

## Routes

1. `COMPOSED_V4_PARENT_REJECTED`;
2. `COMPOSED_V4_POLICY_REJECTED`;
3. `COMPOSED_V4_WORK_REJECTED`;
4. `COMPOSED_V4_CROSS_TRIGGERED`;
5. `COMPOSED_V4_BOUNDARY`;
6. `COMPOSED_V4_5_OF_5_CANDIDATE`.

R56 changes no default, formula, hook cap, tolerance, trial, event, timing,
runtime or production path. Regress R55--R50 exactly.
