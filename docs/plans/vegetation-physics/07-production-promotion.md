# V7 — Production promotion

## Outcome

Promote only the demonstrated trail-tree/forest consumer into current
architecture and contracts. A Proposed SPEC, compiling lab or visual demo is
not production evidence.

## Required evidence

- V0 closure and PASS for `VEGETATION-BEAM-REF-P1`, `VEGETATION-TREE-P1`,
  `VEGETATION-FRACTURE-P1`, `VEGETATION-COUPLING-P1`,
  `VEGETATION-PERSISTENCE-P1` and `VEGETATION-LOD-P1`;
- exact same-target repeats and Windows/Linux canonical-root equality;
- V0 THOTH workload PASS under current Performance V5 methodology;
- game/headless production-command parity and authored static pre-activation
  fallback;
- `content-package`, `play`, `persistence-replay`, affected `platform` and
  conditional `performance` results;
- all failure cases retain the prior complete physical generation.
- ADR-081 composition DAG, exact exchange tuple, scheduled checkpoint epoch,
  worst-case capacity and primary whole-session fault profiles are closed;
- the successor combined `world-dynamics-step` budget row passes.

GPU correspondence is optional and cannot block CPU promotion or grant GPU
authority.

## Smallest contract change

With the actual production consumer, add only exact engine-owned records for:

- structural definition and calibrated wood/wind/coupling profile;
- canonical active/tier state;
- rigid contact-load batch;
- topology and detached-body handoff receipt;
- immutable presentation snapshot;
- the next current-only composite physical checkpoint/save/replay closure.

Exact names, fields and version numbers are assigned in the promotion change.
Do not add a generic solver API, raw node/cell mutation, arbitrary plugin
fracture callback, renderer-driven LOD request or gameplay access to solver
internals.

## Architecture update

Write a narrow Accepted ADR that preserves PhysX as the sole rigid writer and
promotes only the proven living-structure owner/coupling profile. Update
SPEC-02/03/05/21/24/25/26/30/39/40, routing, traceability and the main roadmap
in the same coherent change. Status changes require observable PASS evidence;
missing target/performance evidence remains `NOT_RUN`.
