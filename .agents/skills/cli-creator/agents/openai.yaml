interface:
  display_name: "CLI Creator"
  short_description: "Build the smallest useful durable CLI"
  default_prompt: "Use $cli-creator to build the smallest CLI that completes these repeated jobs, and add optional surface only when demonstrated."
