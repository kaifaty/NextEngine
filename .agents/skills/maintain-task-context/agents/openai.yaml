interface:
  display_name: "Maintain Task Context"
  short_description: "Resume durable state without ceremony"
  default_prompt: "Use $maintain-task-context to recover only the minimum verified context needed for the next primary artifact."
